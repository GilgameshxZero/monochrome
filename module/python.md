# `python`

Building python from source for older versions may run into this issue: <https://github.com/python/cpython/issues/79001>. Need to clean up profiling data before rebuilding. In general it is difficult and requires installation of a lot of build dependencies, so maybe `pyenv` is preferred in the future.
