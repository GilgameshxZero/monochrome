# `rss`

We use `newsboat` on Unix, and `Capy Reader` on Android. `Capy Reader` takes a `subscriptions.xml`, which we generate from `urls`.

```bash
ln -s ~/main.syncthing/monochrome/module/rss/newsboat ~/.newsboat
```
