# `opustags`

`opustags` is the preferred editor on POSIX systems for IDv2/3 tags on `.opus` files. There is no equivalent for Windows.

We consider swapping to <https://github.com/Martchus/tageditor> in the future for cross-platform support on the command line, as opposed to `mp3tag`.
