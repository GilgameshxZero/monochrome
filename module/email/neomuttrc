# Use this file with
# `source ~/main.syncthing/monochrome/module/email/neomuttrc`.
cd ~/main.syncthing/monochrome/module/email/

# GMail folder setup.
set imap_user="yangawesome@gmail.com"
set folder="imaps://imap.gmail.com"
set spoolfile=+INBOX
set record="+[Gmail]/Sent Mail"
set postponed="+[Gmail]/Drafts"
# "delete" messages = "archive". Use `xb` for real delete.
set trash = "+[Gmail]/All Mail"
set imap_authenticators = "oauthbearer:xoauth2"
set imap_oauth_refresh_command = "/usr/share/neomutt/oauth2/mutt_oauth2.py yangawesome_gmail.com.password.token --decryption-pipe cat --encryption-pipe cat"

# Send via `gilgamesh.cc` usually.
set real_name="Yang Yan"
set from="yang@gilgamesh.cc"
unset ssl_force_tls
set smtp_url="smtp://yang@gilgamesh.cc:2525"
# This file automatically sources the relevant passwords.
source "neomuttrc.password"
# Reply from receiving mailbox, if on this list.
set reverse_name
alternates @gilgamesh.cc yangawesome@gmail.com yangilga@mit.edu yangilga@outlook.com yyan99@mit.edu gilgamesh@tfwno.gf gilgameshxzero@icloud.com gilgameshxzero@outlook.com
# Don't include hostname in message ID. This option is relatively new.
# set message_id_format="<%z@gilgamesh.cc>"
set signature=".signature"
# Always quote reply.
set include=yes
# `khard` handles contact queries.
set query_command="echo %s | xargs khard email -d formatted_name --parsable --"

# General and index options.
mailboxes =INBOX =[Gmail]/All\ Mail =[Gmail]/Sent\ Mail =[Gmail]/Drafts =[Gmail]/Spam =[Gmail]/Bin
set editor="/usr/bin/vim"
set header_cache="~/.neomutt/cache/"
set message_cache_dir="~/.neomutt/cache/"
set message_cache_clean=yes
set pager_stop=yes
set sort=reverse-date
set sort_aux=last-date-received
# This is a good sidebar format, but we prefer fullscreen.
# set sidebar_format="%B%?F? [%F]?%* %?N?%N/?%S"
# Display message size, not number of lines, in index.
set index_format="%4C %Z %{%b %d} %-15.15L %4cr %s"
# Check for new mail every 30 seconds (default: 5).
set mail_check=30
set timeout=30
set time_inc=5

# `archive` maildir conditional. Extract from `../../archive.syncthing/module/email/archive.mbox.tar.xz` if desired.
startup-hook "mailboxes -label archive `ls -d ../../local.syncthing/module/email/archive/ 2>/dev/null`"

# Miscellaneous.
# Don't ask for confirmation for delete.
set delete=yes
# Don't display line wrap markers for easy copy out of tmux buffer.
set markers=no
# Don't ask for confirmation on some functions.
set wait_key=no
# Dump with mailcap if html available and no plaintext.
set mailcap_path="mailcap"
auto_view text/html
alternative_order text/plain text/html

# The scroll is a little confusing to me.
bind editor <space> noop

# Search, reply, tab complete contacts.
bind editor <Tab> complete-query
bind editor ^T complete
bind index N search-opposite
bind index \CN toggle-new
bind index,pager R group-reply

# Replacements.
bind index,pager $ noop
macro index,pager $ "<imap-fetch-mail><sync-mailbox>"
bind index,pager <Up> previous-entry
bind index,pager K previous-unread
bind index,pager <Down> next-entry
bind index,pager J next-unread
# bind pager <Up> previous-line
# bind pager <Down> next-line

# Use `d` to mark for "archive", and "$" to sync.
# x-keys are general operations.
bind index,pager x noop
macro index,pager xb "<save-message>=[Gmail]/Bin<enter><enter>"
macro index,pager xp "<purge-message>"
macro index,pager xa "<save-message>=[Gmail]/All^V Mail<enter><enter>"
macro index,pager xi "<save-message>=INBOX<enter><enter>"
macro index,pager xr "<tag-pattern>~U<enter><tag-prefix><clear-flag>N<untag-pattern>.<enter>"
macro index,pager xc "<search>~m<space><1024<space>~B<space>17a7ceae-5204-47cd-8c8a-74b95b3aa83d<enter>"

# Force color.
color normal black white
color hdrdefault black white
color signature black white

# Change to `snowfall` at the end for saved files.
# cd ../../../snowfall/
