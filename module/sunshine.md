# sunshine

For the `2026.05` version of `sunshine-bin` on the AUR, the user `systemd` service is not created automatically. We provide service files here instead. Copy them into `~/.config/systemd/user`.
