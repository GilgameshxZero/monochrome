# gilgamesh-47

The default tunnel to `gilgamesh-31` is disabled, since this is a server machine (and it makes postrouting the OVPN server connections harder). Instead, we enable all split tunnels (except for the `gilgamesh-47` split tunnel, since we run that OVPN server ourselves).
